#include "alt.h"

t_v2f   new_v2f(double x, double y)
{
    t_v2f a;

    a.x = x;
    a.y = y;
    return (a);
}

t_v2    new_v2(int x, int y)
{
    t_v2 a;

    a.x = x;
    a.y = y;
    return (a);
}